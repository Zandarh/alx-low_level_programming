#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: char type lette
  Return: 1 if c is lowercase and 0 otherwise
 */

int _islower(int c)
{
	if (c >= 97 && c <= 123)
	{
		Return (1);
	}
	else
	{
		Return (0);
	}
}


