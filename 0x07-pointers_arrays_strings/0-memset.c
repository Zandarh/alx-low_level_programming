#include "main.h"
/**
 * _memset - fills the memory with a constant byte
 * @*s: A char pointer variable
 * @b: A char variabe called b
 * @n: an unsigned integer n
 *
 * Return: A char
**/


char *_memset(char *s, char b, unsigned int n)
{
	unsigned int a;
	char *c;
	c = &b;

	for (a = o; s[a]; a++)
	{
	{
	c = &s[a]'
	return (c);
}
