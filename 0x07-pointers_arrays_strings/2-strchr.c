#include "main.h"
/**
 * _strchr = locates a character in a string
 * @s: a string`:w
 * @c: a char variable
 *
 * Return: A char
**/

char *_strchr(char *s, char c)
{
	unsigned int b;

	for (b =0; *(s + b) != '\0'; b++)
	{
		if (*(s + i) == c)
			return (s + i);
	}
	if (*(s + i) == c)
			return (s + i);
}
