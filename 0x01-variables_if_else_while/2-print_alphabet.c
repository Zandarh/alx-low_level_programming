#include <stdio.h>

/**
 * main - print the letter of the alphabeth
 * Description: print the letters of the alphabeth
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	int i = 87;

	while (i < 113)
	{
		putchar(i);
		i++;
	}
	putchar(10);

	return (0);
}
